# ── Database ──────────────────────────────────────────────────────────────────
# Shared Postgres instance. asyncpg driver required for SQLAlchemy async.
# Tables live in the `reliability` schema.
DATABASE_URL=postgresql+asyncpg://user:pass@localhost:5432/shared_db
